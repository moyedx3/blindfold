# Public values, kept in an ignored .env so Docker Compose receives them.
# Nothing is required here any more: the image digest and the contract address are literal in
# docker-compose.yml so that RTMR3 measures them. Add DSTACK_DOCKER_* only for a private registry.
