node_modules/
dist/
.DS_Store
*.log
# Midnight local state: wallet seeds, sync state, deployments
.midnight-state.json
.midnight-wallet-state/
.midnight-wallet-state
# compiled Compact artifacts (regenerate with `compact compile`)
**/contracts/managed/
# local secrets
.env
.env.*
!.env.example
# browser spike: regenerate by copying spike/hello/contracts/managed/blindfold after `compact compile`
spike/web/src/contract/compiled/
# midnight-js private-state store (local)
**/midnight-level-db/
# local git worktrees
.worktrees/
# subagent-driven-development scratch
.superpowers/
# compiled contract package output (regenerate with `npm run compile -w contract`)
contract/build/
# buyer app's copy of the compiled contract artifacts (regenerate with `node buyer/scripts/copy-artifacts.mjs`)
buyer/public/contract/
# creator app's copy of the compiled contract artifacts (regenerate at dev/build time)
creator/public/contract/
creator/playwright-report/
creator/test-results/
# Playwright run artifacts
buyer/test-results/
buyer/playwright-report/
# Local demo processes, encrypted content, and creator recovery bundles
.local/
# Redacted live-smoke output (upload deliberately as CI evidence when needed)
deploy/evidence/
